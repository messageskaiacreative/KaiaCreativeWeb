# PDF Extraction Worker — Dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
supabase==2.11.0

# PDF Extraction (primary)
docling==2.14.0

# PDF Extraction (fallback)
PyMuPDF==1.25.1
tabulate==0.9.0
pandas==2.2.3

# HTTP client
httpx==0.28.1

# Production ASGI server
gunicorn==23.0.0
